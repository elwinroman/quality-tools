# ==============================================================================
# Configuración de infraestructura Docker
# Estas variables son usadas SOLO por docker-compose.yml
# ==============================================================================
BACKEND_PORT=3000
CLIENT_PORT=80
DOMAIN=http://localhost
APP_VERSION=2.0.0
